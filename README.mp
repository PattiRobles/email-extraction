# Email Extraction

Filtering out emails from a large data file using RegEx.

Creation of a library to document and count all valid domains encountered in the file, ranking them in order of populatity.

Run using `node .\main.js` in the console.



